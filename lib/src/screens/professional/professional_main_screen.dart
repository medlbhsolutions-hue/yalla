import 'package:flutter/material.dart';

class ProfessionalMainScreen extends StatelessWidget {
  const ProfessionalMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dashboard Professionnel'),
      ),
      body: const Center(
        child: Text('Dashboard Professionnel - En cours de développement'),
      ),
    );
  }
}